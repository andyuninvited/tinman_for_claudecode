# TinMan Status

Check the status of TinMan's heartbeat scheduler and recent results.

## Usage

```
/tinman-status
```

## Instructions

Run `tinman status` to show the current scheduler state (running/stopped, interval, last run time). Then run `tinman logs` to show recent heartbeat history. Summarize the results for the user, highlighting anything that needs attention.
